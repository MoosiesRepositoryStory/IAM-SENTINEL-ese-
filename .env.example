# --- IAM Sentinel configuration ------------------------------------------------
# Copy to .env and adjust. All values have sensible defaults for local/demo use.

# Database. SQLite by default; point at Postgres for the "serious" demo.
# DATABASE_URL=postgresql+psycopg://sentinel:sentinel@db:5432/sentinel
DATABASE_URL=sqlite:///data/sentinel.db

# Where the SQLite DB and JSON/CSV report artifacts live.
DATA_DIR=data
REPORTS_DIR=data/reports

# Flask session signing key — CHANGE for anything internet-facing.
SECRET_KEY=dev-insecure-change-me

# Signing key for /api/v1's JWTs. Independent of SECRET_KEY so a leak of one
# doesn't also forge the other. Falls back to SECRET_KEY when unset (today's
# behavior); set to a distinct value for any internet-facing deployment.
# JWT_SECRET_KEY=

# Set to "production" to make startup fail closed if SECRET_KEY/JWT_SECRET_KEY
# are still the dev default, too short (<32 chars), or identical to each
# other. Anything else (unset, "development", "testing") keeps the lenient
# dev/demo defaults working as-is.
ENVIRONMENT=development

# Canonical external base URL. Set for any internet-facing deployment so
# generated external links (e.g. the ticket-notification deep link sent to
# webhook/Jira/Slack integrations) use a fixed, trusted host instead of
# whatever Host header the incoming request happened to carry. Unset by
# default — local/demo use derives external URLs from the request as before.
# PUBLIC_BASE_URL=https://sentinel.example.com

# Public-demo mode: clamps every RBAC capability above read_only to
# always-denied (see app/services/rbac.py's at_least()), regardless of the
# signed-in account's actual role. Off by default — a normal deployment's
# seeded admin/analyst accounts keep their real capabilities.
# PUBLIC_MODE=true

# Run scans inline (Phase 0/1). Set false in Phase 2 to use the RQ worker.
SYNC_JOBS=true

# --- Phase 2+ (unused until then) ---------------------------------------------
# REDIS_URL=redis://redis:6379/0
